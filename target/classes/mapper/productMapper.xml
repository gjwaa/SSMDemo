<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gjw.mapper.ProductMapper">

    <insert id="addProduce" parameterType="ProductInfo">
        insert into product (productName,startPrice,productTypeID,productImg,introduce,stateID) values (#{productName},#{startPrice},#{productTypeID},#{productImg},#{introduce},#{stateID})
    </insert>

</mapper>